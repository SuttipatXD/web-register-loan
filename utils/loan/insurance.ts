import { InsuranceInfo } from './types';

/** Shared insurance opt-in data — reused as-is across every loan-type data file. */
export const sharedInsuranceInfo: InsuranceInfo = {
  wantInsurance: false,
};
